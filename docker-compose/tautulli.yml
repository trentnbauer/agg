version: '3'
services:
  app:
    image: ghcr.io/linuxserver/tautulli:version-v2.13.1
    environment:
      - PUID=1000
      - PGID=1000
      - TZ=$TZ
    volumes:
      - app:/config
    ports:
      - $PORT:8181
    restart: unless-stopped

volumes:
  app:
